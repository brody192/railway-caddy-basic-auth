# Caddy Basic Auth Proxy

### Needed Variables

- `PROXY_PASS` - The URL of the upstream endpoint, E.g. `http://kibana.railway.internal:8080`
- `USERNAME` - Basic auth username
- `PASSWORD` - Basic auth password